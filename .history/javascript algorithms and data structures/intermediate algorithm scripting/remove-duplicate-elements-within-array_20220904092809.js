// create an array with duplicative values, declare and abbrev log, then chack.
const [arr, log] = [[2, 3, 4, 5, 6, 3, 2, 8, 20], console.log];
// log(arr); // logs [2, 3, 4,  5, 6,3, 2, 8, 20]

// use filter method for comparisons
arr.filter(elem => log(
    elem,
    arr.includes(elem),
    arr.indexOf
))

